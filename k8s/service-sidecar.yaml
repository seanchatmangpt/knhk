apiVersion: v1
kind: Service
metadata:
  name: knhk-sidecar
  namespace: kgc
  labels:
    app: knhk
    component: sidecar
spec:
  type: ClusterIP
  ports:
    - name: grpc
      port: 50051
      targetPort: 50051
      protocol: TCP
  selector:
    app: knhk
    component: sidecar

