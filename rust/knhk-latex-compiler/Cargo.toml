[package]
name = "knhk-latex-compiler"
version = "0.1.0"
edition = "2021"

[[bin]]
name = "compile-latex"
path = "src/main.rs"

[dependencies]
which = "6.0"

[dev-dependencies]
chicago-tdd-tools = { version = "1.3.0", features = ["testing-extras", "otel", "weaver", "testcontainers", "async"] }
